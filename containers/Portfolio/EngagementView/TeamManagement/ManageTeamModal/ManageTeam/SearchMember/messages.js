import { defineMessages } from 'react-intl'

export const scope = 'SearchControl'

export default defineMessages({
  inputPlaceholder: {
    id: `${scope}.InputPlaceholder`,
    defaultMessage: 'Search'
  },
})

import { defineMessages } from 'react-intl'

export const scope = 'HeaderBF'
export default defineMessages({
  backToPortfolioView: {
    id: `${scope}.BackToPortfolioView`,
    defaultMessage: 'Back to portfolio view'
  }
})
import { defineMessages } from 'react-intl'

export const scope = 'MultipleSelectSearch'

export default defineMessages({
  inputPlaceholder: {
    id: `${scope}.InputPlaceholder`,
    defaultMessage: 'Search'
  }
})
import React from 'react'
import { Placeholder } from 'semantic-ui-react'

const UIPlaceholder = (props) => {
  return <div className='wrapper-placeholder'><Placeholder {...props} /></div>
}

export default UIPlaceholder
import React from 'react'
import { Segment } from 'semantic-ui-react'

const UISegment = (props) => {
  return <div className='wrapper-segment'><Segment {...props} /></div>
}

export default UISegment